import streamlit as st

def render():
    st.header("Kiểm thử độc lập có điều kiện (Conditional Independence Test)")

    st.markdown(
        """
        Khai thác các mẫu hình và quan hệ giữa các biến trong dữ liệu là mục tiêu nghiên cứu của nhiều lĩnh vực như toán học thống kê, học máy.
        """)
    st.markdown(
        r"""
        Quan hệ tương quan (correlation) giữa hai biến cho ta biết giá trị của chúng có xu hướng thay đổi cùng nhau.
        Khi biết hai biến $$X$$ và $$Y$$ có quan hệ tương quan, ta có thể nói rằng $$X$$ ảnh hưởng đến $$Y$$ hoặc $$Y$$ ảnh hưởng đến $$X$$,
        hoặc cả hai biến đều bị ảnh hưởng bởi một biến thứ ba $$Z$$.
        """)
    st.markdown(
        r"""
        
        """)